#include<stdio.h>
#include<stdlib.h>
int main()
{
    //else只与离自己最近的if配对
/*     //switch分支语句
    int n = 1, m = 2;
    switch(n)//switch可以嵌套使用
    {
        case 1:
            m++;
        case 2:
            n++;
        case 3:
        switch(n)
        {
            case 1:
                n++;
            case 2:
                n++;
                m++;
                break;
        }
        case 4:
            m++;
            break;
        default:
            break;
    }
    printf("n的值为：%d,\nm的值为：%d\n", n, m); */
    //while循环
    
    system("pause");
    return 0;
} 